<div>
    <ul>
         {{#each artists}}
         <div class="artist">
             <h2>{{ name }}</h2>
            <img src="{{ images.0.url }}" alt="{{ name }}">
            <button><a href="/albums/?id={{ id }}">View Albums</a></button>
         </div>
         {{/each}}
    </ul>
</div>